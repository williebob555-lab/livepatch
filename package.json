{
  "name": "livepatch",
  "productName": "LivePatch",
  "version": "0.1.4",
  "description": "Block-and-wire audio patching environment: Windows audio / ASIO / VST pipelines with a fully customizable node editor.",
  "author": "LivePatch",
  "//repository": "Single source of truth for the update feed: electron-builder derives the GitHub owner/repo for both `--publish` and the app's baked-in app-update.yml from this field. Set it before the first release.",
  "repository": "https://github.com/williebob555-lab/livepatch",
  "main": "electron/main.cjs",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:engine",
    "build:engine": "tsc -p engine/tsconfig.json && node engine/postbuild.mjs",
    "build:vsthost": "node scripts/build-vsthost.mjs",
    "bundle:node": "node scripts/bundle-node.mjs",
    "brand": "electron scripts/make-brand.cjs",
    "typecheck": "tsc --noEmit && tsc -p engine/tsconfig.json --noEmit",
    "start": "npm run build && electron .",
    "electron": "electron .",
    "package": "npm run build:vsthost && npm run build && npm run bundle:node && electron-builder --win",
    "package:dir": "npm run build:vsthost && npm run build && npm run bundle:node && electron-builder --win --dir",
    "release": "npm run build:vsthost && npm run build && npm run bundle:node && electron-builder --win --publish always",
    "ship": "node scripts/ship.mjs",
    "ship:minor": "node scripts/ship.mjs minor",
    "ship:dry": "node scripts/ship.mjs --dry-run"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "cmake-js": "^8.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^26.15.3",
    "node-addon-api": "^8.9.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@julusian/midi": "^3.7.2",
    "audify": "^1.10.1",
    "electron-updater": "^6.8.9",
    "libflacjs": "^5.4.0",
    "wasm-media-encoders": "^0.7.0"
  }
}
